import { EndLetterCapitalPipe } from './end-letter-capital.pipe';

describe('EndLetterCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new EndLetterCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
